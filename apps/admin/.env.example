# JWT auth secret — change this to a random string in production
AUTH_SECRET=mtt-admin-secret-change-in-production-2026

# Vercel KV — add from Vercel Dashboard > Storage > KV
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Vercel Blob — add from Vercel Dashboard > Storage > Blob
BLOB_READ_WRITE_TOKEN=
