import Landing from './Landing';

export const metadata = {
  title: 'FlowStudio — Real footage. Real people. Provably theirs.',
  description:
    'The provenance-native film studio. Drop event footage, let editors cut it, and pay the whole human chain — authorship tracked on Origo, identity verified, privacy kept.',
};

export default function Home() {
  return <Landing />;
}
