// @opennextjs/cloudflare config — deploys Next 15 App Router onto Cloudflare
// Workers (house hard rule: everything on Cloudflare, never Vercel).
import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig();
