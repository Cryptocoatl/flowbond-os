# Database (Supabase / PostgreSQL)
# When set, the app uses the real DB. Without it, mock data is used.
DATABASE_URL=

# FlowBond identity (future)
FLOWBOND_IDENTITY_URL=

# Anthropic (for AI garden features — optional)
ANTHROPIC_API_KEY=
