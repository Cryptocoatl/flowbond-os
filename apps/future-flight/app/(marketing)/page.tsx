import { Landing } from '@/components/Landing'

// Phase 4: fetch the active edition from ff_editions / ff_ticket_tiers / … and
// pass it as <Landing content={...} />. For now it renders defaultContent.
export default function Page() {
  return <Landing />
}
