# FlowBond Life (apps/web) — environment

# Supabase (project fgsrcxxccdjqyrpkitmk) — powers the marketing.waitlist insert.
# Both are safe to expose to the client; never put a service-role key here.
NEXT_PUBLIC_SUPABASE_URL=https://fgsrcxxccdjqyrpkitmk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# FlowEdit visual CMS API (optional in local dev; defaults to http://localhost:4000)
NEXT_PUBLIC_FLOWEDIT_API_URL=
