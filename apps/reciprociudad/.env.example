# Reciprociudad — canonical FlowBond Supabase (Layer 0 root, only)
# Project ref: fgsrcxxccdjqyrpkitmk (us-east-2). NEVER use eoajujwpdkfuicnoxetk (does not exist).
SUPABASE_URL="https://fgsrcxxccdjqyrpkitmk.supabase.co"

# Service role — SERVER-ONLY. Used by app/api/fbid/link to capture joins via a
# SECURITY DEFINER RPC in the `reciprociudad` schema. Never exposed to the client.
# If unset, /api/fbid/link runs in demo mode (validates + returns ok, no DB write).
SUPABASE_SERVICE_ROLE=""

# Optional: public anon key, only needed if/when client-side reads are added.
NEXT_PUBLIC_SUPABASE_URL="https://fgsrcxxccdjqyrpkitmk.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Public site origin (used for OG/canonical metadata).
NEXT_PUBLIC_SITE_URL="https://reciprociudad.lat"
