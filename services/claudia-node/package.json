{
  "name": "@flowbond/claudia-node",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ClaudIA always-on node — a persistent presence that serves the membership across every messaging platform via a Matrix hub.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.37.0",
    "@supabase/supabase-js": "^2.105.4",
    "dotenv": "^16.4.5",
    "matrix-bot-sdk": "^0.7.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
